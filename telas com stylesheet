//tela de login//




import { Text, View, TextInput, Image, Button, Pressable, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>

      <View style={styles.topo}>
        <Text style={styles.titulo}>Login da Fazenda</Text>
      </View>

      <View style={styles.imagemArea}>
        <Image 
          source={require('./logo.png')} 
          style={styles.logo} 
        />
      </View>

      <View style={styles.formulario}>
        <Text style={styles.label}>Insira seu E-mail</Text>
        <TextInput
          placeholder='Seu E-mail'
          style={styles.input}
        />

        <Text style={styles.label}>Insira sua Senha</Text>
        <TextInput
          placeholder='Sua senha'
          keyboardType='numeric'
          secureTextEntry={true}
          style={styles.input}
        />

        <View style={styles.botaoArea}>
          <Button
            title='Entrar'
            onPress={() => { alert("Login realizado") }}
            disabled={false}
          />
        </View>

        <Pressable onPress={() => { alert("mensagem generica 676767") }}>
          <Text style={styles.linkTexto}>Nao tem conta? Cadastre-se</Text>
        </Pressable>
      </View>

      <View style={styles.rodape}>
        <Text style={styles.rodapeTexto}>V1.67-Pedro Henrique Santos</Text>
      </View>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#838C61',
  },
  topo: {
    flex: 15,
    alignItems: 'center',
    justifyContent: 'center',
  },
  titulo: {
    fontSize: 30,
    fontWeight: 'bold',
    color: '#D9C8B4',
    textAlign: 'center',
  },
  imagemArea: {
    flex: 25,
    alignItems: 'center',
    justifyContent: 'center',
  },
  logo: {
    width: 150,
    height: 150,
  },
  formulario: {
    flex: 50,
    paddingHorizontal: 30,
    justifyContent: 'center',
  },
  label: {
    fontSize: 16,
    color: '#D9C8B4',
    textAlign: 'center',
    marginTop: 10,
  },
  input: {
    borderWidth: 1,
    borderColor: '#D9C8B4',
    borderRadius: 5,
    margin: 10,
    padding: 8,
    textAlign: 'center',
    color: '#D9C8B4',
  },
  botaoArea: {
    marginTop: 15,
    marginHorizontal: 40,
  },
  linkTexto: {
    textAlign: 'center',
    color: '#D9C8B4',
    marginTop: 20,
    fontSize: 14,
  },
  rodape: {
    flex: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  rodapeTexto: {
    fontSize: 15,
    fontWeight: 'bold',
    color: '#D9C8B4',
    textAlign: 'center',
  },
});import { Text, View, TextInput, Image, Button, Pressable, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>

      <View style={styles.topo}>
        <Text style={styles.titulo}>Login da Fazenda</Text>
      </View>

      <View style={styles.imagemArea}>
        <Image 
          source={require('./logo.png')} 
          style={styles.logo} 
        />
      </View>

      <View style={styles.formulario}>
        <Text style={styles.label}>Insira seu E-mail</Text>
        <TextInput
          placeholder='Seu E-mail'
          style={styles.input}
        />

        <Text style={styles.label}>Insira sua Senha</Text>
        <TextInput
          placeholder='Sua senha'
          keyboardType='numeric'
          secureTextEntry={true}
          style={styles.input}
        />

        <View style={styles.botaoArea}>
          <Button
            title='Entrar'
            onPress={() => { alert("Login realizado") }}
            disabled={false}
          />
        </View>

        <Pressable onPress={() => { alert("mensagem generica 676767") }}>
          <Text style={styles.linkTexto}>Nao tem conta? Cadastre-se</Text>
        </Pressable>
      </View>

      <View style={styles.rodape}>
        <Text style={styles.rodapeTexto}>V1.67-Pedro Henrique Santos</Text>
      </View>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#838C61',
  },
  topo: {
    flex: 15,
    alignItems: 'center',
    justifyContent: 'center',
  },
  titulo: {
    fontSize: 30,
    fontWeight: 'bold',
    color: '#D9C8B4',
    textAlign: 'center',
  },
  imagemArea: {
    flex: 25,
    alignItems: 'center',
    justifyContent: 'center',
  },
  logo: {
    width: 150,
    height: 150,
  },
  formulario: {
    flex: 50,
    paddingHorizontal: 30,
    justifyContent: 'center',
  },
  label: {
    fontSize: 16,
    color: '#D9C8B4',
    textAlign: 'center',
    marginTop: 10,
  },
  input: {
    borderWidth: 1,
    borderColor: '#D9C8B4',
    borderRadius: 5,
    margin: 10,
    padding: 8,
    textAlign: 'center',
    color: '#D9C8B4',
  },
  botaoArea: {
    marginTop: 15,
    marginHorizontal: 40,
  },
  linkTexto: {
    textAlign: 'center',
    color: '#D9C8B4',
    marginTop: 20,
    fontSize: 14,
  },
  rodape: {
    flex: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  rodapeTexto: {
    fontSize: 15,
    fontWeight: 'bold',
    color: '#D9C8B4',
    textAlign: 'center',
  },
});



//tela splash//

import { Text, View, Image, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <View style={styles.headerContainer}>
        <Text style={styles.title}>
          Fazenda de controle de cogumelos
        </Text>
      </View>

      <Text style={styles.subtitle}>
        Criação certificada de cogumelos
      </Text>

      <View style={styles.spacerSmall} />

      <View style={styles.imageContainer}>
        <Image 
          source={require('./logo.png')} 
          style={styles.logo} 
        />
      </View>

      <View style={styles.spacerMedium} />

      <Text style={styles.footerText}>
        V1.67-Pedro Henrique Santos
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#838C61',
  },
  headerContainer: {
    flex: 11,
    backgroundColor: '#838C61',
    alignItems: 'center',
    justifyContent: 'center',
  },
  title: {
    fontSize: 23,
    fontWeight: 'bold',
    marginTop: 10,
    color: '#D9C8B4',
    textAlign: 'center',
  },
  subtitle: {
    fontSize: 20,
    fontWeight: 'bold',
    marginTop: 10,
    color: '#D9C8B4',
    textAlign: 'center',
  },
  spacerSmall: {
    flex: 10,
    backgroundColor: '#838C61',
  },
  imageContainer: {
    flex: 64,
    backgroundColor: '#838C61',
    alignItems: 'center',
  },
  logo: {
    width: 340,
    height: 350,
  },
  spacerMedium: {
    flex: 8,
    backgroundColor: '#838C61',
  },
  footerText: {
    fontSize: 15,
    fontWeight: 'bold',
    marginTop: 10,
    color: '#D9C8B4',
    textAlign: 'center',
  },
});

//tela home//


import { Text, View, Image, Button, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>

      <View style={styles.topo}>
        <Text style={styles.titulo}>Painel da Fazenda</Text>
      </View>

      <View style={styles.imagemArea}>
        <Image 
          source={require('./logo.png')} 
          style={styles.logo} 
        />
      </View>

      <View style={styles.infoArea}>
        <Text style={styles.infoTexto}>Cogumelos em producao: 1250</Text>
        <Text style={styles.infoTexto}>Temperatura: 22 graus</Text>
        <Text style={styles.infoTexto}>Umidade: 85%</Text>
        <Text style={styles.infoTexto}>Estoque: 340 kg</Text>
      </View>

      <View style={styles.botaoArea}>
        <Button
          title='Atualizar Dados'
          onPress={() => { alert("Dados atualizados") }}
          disabled={false}
        />
      </View>

      <View style={styles.rodape}>
        <Text style={styles.rodapeTexto}>V1.67-Pedro Henrique Santos</Text>
      </View>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#838C61',
  },
  topo: {
    flex: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  titulo: {
    fontSize: 25,
    fontWeight: 'bold',
    color: '#D9C8B4',
    textAlign: 'center',
  },
  imagemArea: {
    flex: 40,
    alignItems: 'center',
    justifyContent: 'center',
  },
  logo: {
    width: 200,
    height: 200,
  },
  infoArea: {
    flex: 35,
    alignItems: 'center',
    justifyContent: 'center',
  },
  infoTexto: {
    fontSize: 18,
    color: '#D9C8B4',
    marginTop: 10,
    textAlign: 'center',
  },
  botaoArea: {
    flex: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  rodape: {
    flex: 5,
    alignItems: 'center',
    justifyContent: 'center',
  },
  rodapeTexto: {
    fontSize: 15,
    fontWeight: 'bold',
    color: '#D9C8B4',
    textAlign: 'center',
  },
});import { Text, View, Image, Button, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>

      <View style={styles.topo}>
        <Text style={styles.titulo}>Painel da Fazenda</Text>
      </View>

      <View style={styles.imagemArea}>
        <Image 
          source={require('./logo.png')} 
          style={styles.logo} 
        />
      </View>

      <View style={styles.infoArea}>
        <Text style={styles.infoTexto}>Cogumelos em producao: 1250</Text>
        <Text style={styles.infoTexto}>Temperatura: 22 graus</Text>
        <Text style={styles.infoTexto}>Umidade: 85%</Text>
        <Text style={styles.infoTexto}>Estoque: 340 kg</Text>
      </View>

      <View style={styles.botaoArea}>
        <Button
          title='Atualizar Dados'
          onPress={() => { alert("Dados atualizados") }}
          disabled={false}
        />
      </View>

      <View style={styles.rodape}>
        <Text style={styles.rodapeTexto}>V1.67-Pedro Henrique Santos</Text>
      </View>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#838C61',
  },
  topo: {
    flex: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  titulo: {
    fontSize: 25,
    fontWeight: 'bold',
    color: '#D9C8B4',
    textAlign: 'center',
  },
  imagemArea: {
    flex: 40,
    alignItems: 'center',
    justifyContent: 'center',
  },
  logo: {
    width: 200,
    height: 200,
  },
  infoArea: {
    flex: 35,
    alignItems: 'center',
    justifyContent: 'center',
  },
  infoTexto: {
    fontSize: 18,
    color: '#D9C8B4',
    marginTop: 10,
    textAlign: 'center',
  },
  botaoArea: {
    flex: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  rodape: {
    flex: 5,
    alignItems: 'center',
    justifyContent: 'center',
  },
  rodapeTexto: {
    fontSize: 15,
    fontWeight: 'bold',
    color: '#D9C8B4',
    textAlign: 'center',
  },
});
